__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.claude/
*.tmp
*_preview.html
bermuda_vs_padspan_comparison.txt
# Raw local screen captures — curated doc assets in images/ are added by name
Screenshot *.png
# Local pre-deploy store backups — never publish
backups/
# Working notes containing LAN addresses and deploy commands — never publish
docs/fabric-independence-plan-*.md
docs/fabric-simplification-pass*.md
